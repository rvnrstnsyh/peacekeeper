/**
 * Shared Interface Definitions
 *
 * Place cross-cutting interfaces that are reused across multiple modules here.
 * Module-specific interfaces belong in their own module's types or dto folder.
 *
 * Examples of what belongs here:
 *   - Paginated<T> — generic pagination wrapper
 *   - Auditable — createdAt / updatedAt / deletedAt base
 *   - Repository<T> — base CRUD contract for all repositories
 */
